# ## 核心功能
# 声明本地机会匹配 CLI 包和版本。
# ## 输入
# 包导入请求。
# ## 输出
# 包版本号 `__version__`。
# ## 定位
# `opportunity_matcher` 源码包入口，不承载业务流程。
# ## 依赖
# 无内部业务依赖。
# ## 维护规则
# 版本变化或包入口职责变化时同步更新本说明。

__version__ = "0.1.0"
